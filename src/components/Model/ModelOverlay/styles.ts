import styled from 'styled-components';

export const Container = styled.div`
    position: sticky;
    top: 0;
    height: 100vh;
    margin-top: -100vh;

    // centralização
    display: flex;
    flex-direction: column;
    align-items: center;
`